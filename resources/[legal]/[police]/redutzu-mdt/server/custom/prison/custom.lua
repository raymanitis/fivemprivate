if Config.Prison ~= 'custom' then
    return
end

function JailOfflinePlayer(identifier, amount)
    -- Implement your own code
end

function JailPlayer(identifier, amount, sender, reason)
    -- Implement your own code
end