return {
    startingMMR = 1000, -- The starting MMR for new racers
    mmrKFactor = 64, -- The factor by which the MMR will be changed. Lower values will result in slower MMR changes,
                    -- higher values will result in faster MMR changes. The default value is 32.
}