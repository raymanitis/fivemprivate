Config = Config or {}

Config.AdminGroup = { 'group.admin' } -- Used to restrict the minigames menu to admins

Config.DevMenuCommand = 'minigames-menu'

-- UI Configuration
Config.HideInstructions = false -- Set to true to hide game instructions across all minigames
Config.MinimalMode = true      -- Set to true to hide everything except game area and progress bars

Config.CheckForUpdates = true
